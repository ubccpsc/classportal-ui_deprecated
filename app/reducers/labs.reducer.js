import * as types from '../actions/types.helper';
import initialState from './initial.state';

// export default function labsReducer(state = initialState.labs, action) {
//   switch(action.type) {
//     case types.GET_LABS_LIST_FROM_COURSE_FULFILLED:
//       return action.payload.response;
//     default: 
//       return state;
//   }
// }